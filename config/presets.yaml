presets:
  # ─────────────────────────────────────────────────────────────
  # H.265 10-bit — 1080p presets
  # ─────────────────────────────────────────────────────────────
  mkv-1080p-h265-10bit-medium-cq20-all-audio:
    display_name: "MKV - 1080p - H265 10-bit - Medium CQ20 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 20
      preset: medium
      max_width: 1920
      max_height: 1080
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-1080p-h265-10bit-medium-cq25-all-audio:
    display_name: "MKV - 1080p - H265 10-bit - Medium CQ25 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 25
      preset: medium
      max_width: 1920
      max_height: 1080
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-1080p-h265-10bit-medium-cq30-all-audio:
    display_name: "MKV - 1080p - H265 10-bit - Medium CQ30 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 30
      preset: medium
      max_width: 1920
      max_height: 1080
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  # ─────────────────────────────────────────────────────────────
  # H.265 10-bit — native resolution presets (no scaling)
  # ─────────────────────────────────────────────────────────────
  mkv-h265-10bit-medium-cq20-all-audio:
    display_name: "MKV - H265 10-bit - Medium CQ20 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 20
      preset: medium
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-h265-10bit-medium-cq21-all-audio:
    display_name: "MKV - H265 10-bit - Medium CQ21 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 21
      preset: medium
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-h265-10bit-medium-cq25-all-audio:
    display_name: "MKV - H265 10-bit - Medium CQ25 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 25
      preset: medium
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-h265-10bit-medium-cq30-all-audio:
    display_name: "MKV - H265 10-bit - Medium CQ30 - All Audio Passthru"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 30
      preset: medium
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  # ─────────────────────────────────────────────────────────────
  # H.265 10-bit — 720p BT.709 presets (language-specific audio)
  # ─────────────────────────────────────────────────────────────
  mkv-720p-h265-10bit-bt709-faster-rf25-dutch:
    display_name: "MKV - 720p - H265 10-bit - BT.709 - Faster RF25 - Dutch Audio"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 25
      preset: faster
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
    audio:
      mode: transcode
      codec: aac
      bitrate: "160k"
      language: nld
    subtitles: first

  mkv-720p-h265-10bit-bt709-faster-rf25-english:
    display_name: "MKV - 720p - H265 10-bit - BT.709 - Faster RF25 - English Audio"
    container: mkv
    video:
      codec: libx265
      profile: main10
      crf: 25
      preset: faster
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
    audio:
      mode: transcode
      codec: aac
      bitrate: "160k"
      language: eng
    subtitles: first

  # ─────────────────────────────────────────────────────────────
  # H.265 10-bit — 5K presets (MP4 container)
  # ─────────────────────────────────────────────────────────────
  mp4-5k-h265-10bit-slow-rf20-all-audio:
    display_name: "MP4 - 5k - H265 10-bit - Slow RF20 - All Audio Passthru"
    container: mp4
    video:
      codec: libx265
      profile: main10
      crf: 20
      preset: slow
      max_width: 5400
      max_height: 2700
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: none

  mp4-5k-h265-10bit-medium-rf22-all-audio:
    display_name: "MP4 - 5k - H265 10-bit - Medium RF22 - All Audio Passthru"
    container: mp4
    video:
      codec: libx265
      profile: main10
      crf: 22
      preset: medium
      max_width: 5400
      max_height: 2700
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: none

  mp4-5k-h265-10bit-medium-rf23-all-audio:
    display_name: "MP4 - 5k - H265 10-bit - Medium RF23 - All Audio Passthru"
    container: mp4
    video:
      codec: libx265
      profile: main10
      crf: 23
      preset: medium
      max_width: 5400
      max_height: 2700
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: none

  # ─────────────────────────────────────────────────────────────
  # VP9 — 720p BT.709 presets (WebM container)
  # ─────────────────────────────────────────────────────────────
  webm-720p-vp9-bt709-medium-rf33-all-audio:
    display_name: "WEBM - 720p - VP9 - BT.709 - Medium RF33 - All Audio Passthru"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 33
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: passthrough
    subtitles: none

  webm-720p-vp9-bt709-medium-rf36-all-audio:
    display_name: "WEBM - 720p - VP9 - BT.709 - Medium RF36 - All Audio Passthru"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: passthrough
    subtitles: none

  webm-720p-vp9-bt709-slow-rf36-all-audio:
    display_name: "WEBM - 720p - VP9 - BT.709 - Slow RF36 - All Audio Passthru"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 1
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: passthrough
    subtitles: none

  # ─────────────────────────────────────────────────────────────
  # H.264 — 720p BT.709 preset
  # ─────────────────────────────────────────────────────────────
  mkv-720p-h264-bt709-medium-rf25-english:
    display_name: "MKV - 720p - H264 - BT.709 - Medium RF25 - English Audio"
    container: mkv
    video:
      codec: libx264
      crf: 25
      preset: medium
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: transcode
      codec: aac
      bitrate: "160k"
      language: eng
    subtitles: first

  # ─────────────────────────────────────────────────────────────
  # AV1 (SVT-AV1) presets
  # ─────────────────────────────────────────────────────────────
  mkv-1080p-av1-crf22-all-audio:
    display_name: "MKV - 1080p - AV1 - CRF22 - All Audio Passthru"
    container: mkv
    video:
      codec: libsvtav1
      crf: 22
      preset: 6
      max_width: 1920
      max_height: 1080
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-1080p-av1-crf28-all-audio:
    display_name: "MKV - 1080p - AV1 - CRF28 - All Audio Passthru"
    container: mkv
    video:
      codec: libsvtav1
      crf: 28
      preset: 6
      max_width: 1920
      max_height: 1080
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  mkv-720p-av1-crf28-all-audio:
    display_name: "MKV - 720p - AV1 - CRF28 - All Audio Passthru"
    container: mkv
    video:
      codec: libsvtav1
      crf: 28
      preset: 6
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
    audio:
      mode: passthrough
    subtitles: all

  # ─────────────────────────────────────────────────────────────
  # Xpeng playability test presets
  # Testing all permutations: container, colorspace, fps mode
  # VP9 only (AV1 confirmed not direct-playable in Plex Web)
  # ─────────────────────────────────────────────────────────────

  # --- WebM container ---
  xpeng-test-webm-plain:
    display_name: "XPENG - webm"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-webm-bt709:
    display_name: "XPENG - webm bt709"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-webm-cfr:
    display_name: "XPENG - webm cfr"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      fps_mode: cfr
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-webm-bt709-cfr:
    display_name: "XPENG - webm bt709 cfr"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
      fps_mode: cfr
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  # --- MKV container ---
  xpeng-test-mkv-plain:
    display_name: "XPENG - mkv"
    container: mkv
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-mkv-bt709:
    display_name: "XPENG - mkv bt709"
    container: mkv
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-mkv-cfr:
    display_name: "XPENG - mkv cfr"
    container: mkv
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      fps_mode: cfr
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  xpeng-test-mkv-bt709-cfr:
    display_name: "XPENG - mkv bt709 cfr"
    container: mkv
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
      fps_mode: cfr
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  # ─────────────────────────────────────────────────────────────
  # Jellyfin / Xpeng production presets (AV1, WebM, Opus)
  # ─────────────────────────────────────────────────────────────
  jf-720p-av1-crf35-opus:
    display_name: "JF - 720p - AV1 - CRF35 - Audio Passthru"
    container: webm
    video:
      codec: libsvtav1
      crf: 35
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: passthrough
    subtitles: none

  jf-720p-av1-crf32-opus:
    display_name: "JF - 720p - AV1 - CRF32 - Audio Passthru"
    container: webm
    video:
      codec: libsvtav1
      crf: 32
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: passthrough
    subtitles: none

  jf-720p-av1-crf38-opus:
    display_name: "JF - 720p - AV1 - CRF38 - Audio Passthru"
    container: webm
    video:
      codec: libsvtav1
      crf: 38
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: passthrough
    subtitles: none

  # ─────────────────────────────────────────────────────────────
  # Jellyfin playability test presets
  # Jellyfin Web direct-plays AV1 in MP4/WebM/MKV
  # Testing VP9 + AV1 across containers, all with BT.709
  # ─────────────────────────────────────────────────────────────

  # --- VP9 ---
  jf-test-vp9-webm:
    display_name: "JF - vp9 webm"
    container: webm
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  jf-test-vp9-mkv:
    display_name: "JF - vp9 mkv"
    container: mkv
    video:
      codec: libvpx-vp9
      crf: 36
      speed: 2
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  # --- AV1 (SVT-AV1 preset 8, good quality/speed balance) ---
  jf-test-av1-webm:
    display_name: "JF - av1 webm"
    container: webm
    video:
      codec: libsvtav1
      crf: 36
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  jf-test-av1-mkv:
    display_name: "JF - av1 mkv"
    container: mkv
    video:
      codec: libsvtav1
      crf: 36
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  jf-test-av1-mp4:
    display_name: "JF - av1 mp4"
    container: mp4
    video:
      codec: libsvtav1
      crf: 36
      preset: 8
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: aac
      bitrate: "128k"
    subtitles: none

  # --- AV1 Fast (SVT-AV1 preset 10, fastest) ---
  jf-test-av1fast-webm:
    display_name: "JF - av1fast webm"
    container: webm
    video:
      codec: libsvtav1
      crf: 36
      preset: 10
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  jf-test-av1fast-mkv:
    display_name: "JF - av1fast mkv"
    container: mkv
    video:
      codec: libsvtav1
      crf: 36
      preset: 10
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: libopus
      bitrate: "128k"
    subtitles: none

  jf-test-av1fast-mp4:
    display_name: "JF - av1fast mp4"
    container: mp4
    video:
      codec: libsvtav1
      crf: 36
      preset: 10
      max_width: 1280
      max_height: 720
      pix_fmt: yuv420p10le
      colorspace: bt709
    audio:
      mode: transcode
      codec: aac
      bitrate: "128k"
    subtitles: none
